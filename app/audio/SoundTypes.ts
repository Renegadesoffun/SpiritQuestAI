export type SoundPath = {
  'instruments/strings/violin/C4': {
    path: string;
    usage: 'GameSoundDirector.playInstrument';
    format: {
      codec: string;
      sampleRate: number;
      channels: number;
    };
  };
  // ... other sound paths
}; 